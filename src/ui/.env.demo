# Loaded by Vite for `--mode demo` (npm run dev -- --mode demo / vite build --mode demo).
# Turns on the in-memory mock adapter client-side without needing the ?demo query param,
# matching the server-side proxy drop in vite.config.ts (isDemo). Keep in sync with that.
VITE_DEMO=true
